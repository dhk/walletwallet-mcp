services:
  - type: web
    name: walletwallet-mcp
    runtime: docker
    plan: starter
    envVars:
      - key: WALLETWALLET_API_KEY
        sync: false
      - key: MCP_AUTH_TOKEN
        sync: false
